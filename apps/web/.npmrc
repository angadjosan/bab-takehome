# @privy-io/react-auth -> optional peer permissionless -> optional peer ox@0.8 conflicts with viem's ox;
# none of that chain is used (no Privy smart wallets), so peers are not auto-installed.
legacy-peer-deps=true
